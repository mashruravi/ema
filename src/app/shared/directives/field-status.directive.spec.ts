import { FieldStatusDirective } from './field-status.directive';

describe('FieldStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldStatusDirective();
    expect(directive).toBeTruthy();
  });
});
